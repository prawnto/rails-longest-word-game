<h1>New Game</h1>
<%= @letters %>

<p>What is the longest word you can find?</p>
<form action="/score" method="post" data-turbo="false">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <input type="text" name="word">
  <input type="submit" value="Play">
  <input type="hidden" value="<%= @letters %>" name="letters">
</form>

# assigning value="<%= @letters %>" will allow the letters in /new to be sent to the /score
# using the type="hidden" will hide input
# giving a name will allow games_controller to retrieve it as a params
